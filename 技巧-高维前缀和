// 有c维
// 一维一维的做
// 枚举到 j 说明已经完成 0-j 维的前缀和了
for(int j=0;j<c;++j) {
		for(int i=0;i<(1<<c);++i) {  //枚举所有
			if(i&(1<<j)) {  //如果 i 这个数第 j 维是 1，那么加上这个数第 j 维是 0 的那个数组的数
                      //静态可以这样处理
				f[i] += f[i ^ (1<<j)];
			}
		}
	}
